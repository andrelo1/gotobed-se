#pragma once

namespace Version {
	inline constexpr std::size_t MAJOR = ${${PROJECT_NAME}_VERSION_MAJOR};
	inline constexpr std::size_t MINOR = ${${PROJECT_NAME}_VERSION_MINOR};
	inline constexpr std::size_t PATCH = ${${PROJECT_NAME}_VERSION_PATCH};
	inline constexpr std::size_t BUILD = ${${PROJECT_NAME}_VERSION_TWEAK};
}

namespace Project {
	inline constexpr std::string_view NAME = "${PROJECT_NAME}";
}